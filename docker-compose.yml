services:

  hotel-booking-app:
    image: hotel-booking-app:latest
    container_name: hotel-booking-app
    ports:
      - "8080:8080"

    environment:
      SPRING_PROFILES_ACTIVE: docker

    restart: always